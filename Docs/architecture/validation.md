# Architecture validation

Status: normative.

<!-- decision-ref: architecture.decision-coherence -->

For v21 Saved Scene and Queue tests, “tracks” refers only to the separately typed embedded 3D animation record. No validation may imply that v21 has a generic parameter-track collection.

Every cross-boundary product decision touched by a change first resolves through `architecture/decision-authority.json`. The registry has one owner marker and one bounded scope per decision, while marked secondary documents are references only. Validation rejects duplicate, missing or inactive owners, unknown references and absent focused enforcement. A semantic conflict cannot be repaired by selecting one statement automatically: implementation remains blocked until the product decision and its canonical owner agree.

Sensor owner coverage requires that the explicit collected-charge, coupled-charge and quantized-RAW artifact chain produce exactly the same integer codes, clipping masks and profile identity as the complete capture composition.

Physical-artifact contract coverage requires every boundary to use one Application-owned `PhysicalArtifactId`, every stable versioned id to be unique, and every adjacent phase output identity to equal the following input identity. The native bridge may serialize `stable_id()` for a host but cannot redeclare artifact names or represent a boundary as an arbitrary string. CPU checkpoint coverage requires a closed typed artifact result; RAW phases must retain their exact integer codes, Bayer/profile identity and clipping masks, while any normalized RGBA diagnostic is accessed only as presentation and never compared as the canonical RAW boundary.

Device VFX Transparency coverage uses an authored alpha gradient and proves that alpha is a non-color sidecar through feeder placement, geometry and lens sampling. `ignore` must reproduce the opaque physical result; `device-transparency` must attenuate emission before spread/glow and publish premultiplied additive RGB with a separate occlusion matte. Alpha one is exact opaque identity, alpha zero publishes no Device contribution and reveals only the reference plate, and fractional alpha is composed exactly once as `DeviceRGB + plateRGB × (1 − matte)`. CPU/Metal parity covers both modes. Native presentation tests use deliberately non-geometric mattes to prove Swift consumes the Application result rather than rebuilding Device coverage.

Fusion Scene Package coverage proves the headless publication is the normal Camera Rendered ACEScg path on a frontal virtual sensor raster, preserving complete Device RGB at matte values 0, 0.5 and 1 without a second premultiplication and never reading Reference or an ideal Source to reconstruct radiometry. It keeps panel through Camera Rendering Intent, including shutter exposure and lens character; it disables only perspective, Brown distortion, motion accumulation and, when selected, DOF. Maximum-projected-density resolution uses the complete unclipped Device over every exported camera sample, including projections outside the Delivery Raster. Before the first frame, one sequence-wide uniform padding is resolved from that maximum Device sampling density and the declared physical panel-tail, full positive glow, optional baked-DOF and radiometric-fade supports. Every frame is evaluated exactly once; the fixed support is never enlarged by inspecting a filter tail, and only the exterior fade band attenuates RGB toward zero without changing supported spill, the active Device or occlusion matte. Tests prove the fixed support, exterior fade and single frame-provider call per output frame; package-relative media/comp/metadata paths; the EXR Loader is a numbered file sequence rather than a multiframe file; the QuickTime reference Loader has explicit length, trim and global range; its standard ColorSpaceTransform uses `REC709_GAMMA`, `ACES_AP1_COLORSPACE` and `TM_NONE`; the RGB Renderer3D alone requests 32-bit float maximum texture depth while the matte renderer retains the default; no STMap; paired opaque RGB/matte 3D carriers; explicit physical composite math; DOF baked versus Fusion OpenGL accumulation; motion blur delegated with shutter angle/phase; DE4 distortion outside the EXR; and failure for unrepresentable Brown terms. The saved animated-camera integration renders all 100 physical frames, and its generated composition must open in Fusion 21 with every required registered tool. Queue tests prove every sequence owns a directory, empty directory trees are accepted, populated destinations prompt before enqueue, `FailIfExists` blocks, and `ReplaceGeneratedFiles` authorizes only the exact manifest without clearing unrelated files.

Fusion reference coverage proves the generated composition contains no OCIO node, configuration path or copied `.ocio` resource; its Rec.709 plate uses Fusion's standard `ColorSpaceTransform` with `REC709_GAMMA`, `ACES_AP1_COLORSPACE`, HDR-standard conversion and Rec.2390 scaling exactly authored. The Device EXR receives no color conversion. Another authored reference transform does not block queueing, remains recorded by id and produces that named node explicitly disabled with `PassThrough = true`; no substitute transform is selected.

Delivery Raster coverage proves explicit preset materialization, transition to `custom` after a manual edit, current-camera-raster resolution without value inference, exact centered `1:1` crop/pad, aspect-preserving `Fit`, aspect-preserving centered `Fill / Crop`, explicit transparent/black alpha, finite linear ACEScg and strict adjacency from Camera Rendering Intent through Device VFX Transparency and Delivery Raster to Recording Output. Setup coverage proves that focal length, sensor gate, lens shift, camera/screen pose and Delivery Raster determine the same ideal framing without evaluating the physical phases; its one-pixel red boundary must remain attached to the projected active Device rectangle. Environment Setup extends the same ideal mirror plane across the complete Viewer so adjacent reflected directions remain visible for placement: supersampled projected Device coverage retains unit gain inside and a fixed 0.20 diagnostic presentation gain outside, while the red boundary marks the finite Device area consumed by the physical render. It must retain the exact authored finite-sphere center/radius intersection, reject a sphere that does not enclose camera aperture and Device, preserve camera-local cursor conventions for translation/rotation/radius gestures, must not replace or expand the actual Cover/Environment support, and the outside dimming must not alter the selected environment or physical result. Match coverage requires focal length to be explicit authoring inside the selected lens template's declared `0.5×–2×` nominal interval, directly draggable 2D targets and their yellow joining quadrilateral to remain invariant during pose/focal solving, fixed-focal solving to use the authored value, automatic search to minimize reprojection RMS and commit focal plus pose as one undoable edit, and the displayed maximum error to be recomputed through the selected camera's distortion and Delivery Raster mapping. Every phase descriptor publishes a nonempty native calculation domain and the explicit Preview ODT presentation route.

Viewer validation proves that the Setup boundary is drawn after viewer scaling with a constant one-logical-pixel stroke and is absent from rendered textures and exports. Environment resource validation rejects a missing scene-referred IDT, non-positive unit radiance, non-finite/out-of-range exposure, undecodable media and any raster that is not exactly equirectangular 2:1; the prepared image resource must be passed to Cover/Environment and must be released when a procedural preset is selected.

Environment Setup coverage requires the exact authored camera/Device projection, selected HDRI, X/Y rotation and distant/finite-sphere mode; its output is a perfect mirror independent of cover roughness, microtexture, panel and sensor. Dolly changes only the radius of a sphere centred at the scene origin. Application publishes and enforces a dynamic minimum that strictly encloses the complete camera aperture and every active Device corner across every exact frame of an animated timeline; entering Setup publishes one enlarged authored radius when necessary, while invalid direct render requests still fail rather than showing the sphere exterior. Repeated input is deterministic, translating the camera/Device rig relative to the origin changes finite-sphere parallax, and the final physical evaluator consumes the same projection identity, centre and radius.

Source Adjustment validation requires exact neutral identity for negative and above-one ACEScg values, unchanged alpha, finite owner-published limits and CPU/Metal agreement. Environment adjustment requires a selected image source, non-negative finite output after chroma compression, unchanged radiometric ownership and deterministic preparation of the importance hierarchy. Hidden adjustment values on procedural environments are rejected rather than retained.

Capture-preset coverage requires exactly three unique stable `Full`, `Half` and `Quarter` raster identities with explicit nonzero dimensions and an explicit contained default; unknown modes fail at the Application boundary.

Architecture is enforced rather than inferred from prose. `architecture/domains.json` is the exact current workspace package and local-dependency matrix. `architecture/validation-owners.json` assigns every repository path to one validation owner. An undeclared package, dependency or path is an error.

Domain isolation is compiler-first. Rust crate dependencies and Swift target dependencies are the executable authority for which imports can exist. Core and Application targets cannot depend on host, UI, Platform implementation or OFX targets. Presentation targets depend only on presentation contracts and host UI frameworks; granting a Color, Media, Panel, Geometry, Camera, Sensor, Persistence or native-bridge dependency to a presentation target is an architecture failure. Source scanning is used only for invariants that the language dependency graph cannot express.

Model-authoring contract coverage requires unique stable phase and parameter ids, explicit versioned input/output artifact kinds, finite ordered scalar bounds and explicit units. Device preset coverage requires a nonempty unique set of Color Mode ids, an authored default contained in that set and finite ordered luminance capability containing its reference value. Simulation-request coverage proves the selected Color Mode belongs to the selected Device capability and White Luminance lies inside its range without mutating the preset. Every physical request carries an explicit full-raster extent, bounded render window, exact rational X/Y render scale and exact rational pixel aspect. The current evaluator accepts only the declared complete-frame window, unit scale and square pixels and rejects every unsupported host context rather than ignoring it. Host-conformance coverage will evaluate the same resolved request through partitioned render windows when that capability becomes canonical and declares temporal input requirements explicitly. Adapter-specific GPU tests remain adapter tests and cannot make that backend a core dependency.

Every Application phase descriptor publishes its optional exact physical intermediate through the current Test ABI. The host consumes that value verbatim and cannot reconstruct a mapping from phase identity, label, order or Preview-result enum. Application also publishes one ordered physical-stage catalog containing domain, control semantics, certified visual/safe bounds, exact-zero identity and General-overview membership. Application alone validates and resolves the complete ordered contribution set, including Capture dependencies, into named values; the native bridge only verifies ABI identity and translates those resolved values into the immutable plan. The host request carries only the stable stage id and authored continuous or discrete value; it cannot echo, override or validate a second copy of those model semantics.

Test-authoring coverage requires `Origin`, `Source Adjustment`, `Feeder Output`, `Device Mapping and Interpretation`, `Panel Structure`, `Panel Uniformity`, `Panel Light Spread`, `Panel Temporal`, `Relative Geometry`, `Cover and Environment`, `Device Emission Glow`, `Lens and Projection`, `Exposure and Shutter`, `Computational Capture`, `Photosite/CFA Collection and Noise`, `Sensor Bloom`, `Sensor Readout and RAW`, `Develop and Demosaic` and `Camera Rendering Intent` in that exact order, with exact artifact equality at every adjacent boundary, an explicit default cumulative Preview phase and a typed Preview result for every phase. Selecting each phase must set its exact cumulative evaluator intermediate, including Origin, so stale Preview routing is impossible. Relative Geometry is a typed data checkpoint and may retain the preceding raster in Preview without fabricating a visualization. It publishes mutually exclusive Look At and Free control sets: Look At includes camera distance and orbital orientation X/Y; Free includes camera and screen translation XYZ and rotation XYZ. Mode conversion preserves the resolved pose in the forward direction and derives the Look At orbit while explicitly retargeting the screen center in the reverse direction. The ABI contribution mapping proves Geometry and Environment are independent: Geometry one with Environment zero must still project through the authored camera, while Environment one with Geometry zero cannot activate projection. Placement belongs to Feeder Output and must not appear in the Device phase. Panel Structure, Panel Uniformity, Panel Light Spread, Cover, Environment, Device Emission Glow, Lens, Shutter, Computational Capture, Photosite/CFA Collection and Noise, and Sensor Bloom own finite owner-published scalars with zero identity, calibrated physical value one and their certified maxima. Collection publishes `collected-sensor-charge-v1`; Bloom consumes it exactly and publishes `coupled-sensor-charge-v1`; Readout consumes that exact charge and alone publishes `raw-mosaic-noisy-v1`. Develop and Demosaic alone publishes the developed ACEScg camera artifact; Camera Rendering Intent then consumes it exactly and publishes `camera-rendered-acescg-v1`. Every scalar descriptor owns finite ordered input bounds, a slider step equal to five percent of that range and an explicit in-range reset value. Every choice owns an explicit valid reset id. Numeric input is range validated but is not quantized to the slider step. A display-referred Input Transform seeds an equivalent Output Signal only when Application creates a new selection; the resulting authored values are independent. Changing Device or Color Mode cannot mutate Output Signal. A Device change is one Application intent that atomically returns its preset default Color Mode, reference White Luminance, spatial-uniformity profile, light-spread profile and cover defaults. A Camera change returns its default compatible lens; fixed cameras expose exactly that one lens while interchangeable cameras expose their declared compatible set. The host may not reconstruct dependent defaults, reset targets, camera/lens compatibility, placement choices, Preview qualities or published options.

Panel Structure validation treats intensity and moiré saturation as independent finite owner-published controls. Saturation one preserves the sampled residual exactly; zero preserves its ACEScg luminance and removes only chroma.

The exact Computational Capture adjacency is `computational-capture-exposure-v2` from Computational Capture into Photosite/CFA Collection and Noise. Coverage proves one exposure and Character zero are complete identity, eight exposures at Character one add three effective fused-quantization bits, the result caps at sixteen, and CPU and Metal develop the exact effective profile carried through charge into the RAW artifact.

The exact final camera adjacency is `developed-camera-acescg-v1` into Camera Rendering Intent and `camera-rendered-acescg-v1` out. Contract coverage requires complete finite exposure, contrast, saturation, temperature and tint values from the selected capture preset, rejects out-of-range authored values, and proves the iPhone 16e preset resolves `+0.5 EV`, `1.10` contrast, `1.25` saturation, `6500 K` and zero tint. CPU and product Metal compare this cumulative checkpoint within `3e-3` maximum absolute channel difference; the preceding Developed checkpoint remains independently selectable and unchanged.

Panel-uniformity coverage proves zero is bit-exact identity, the fixed field remains attached to physical panel coordinates and frame invariant, calibrated and exaggerated gains remain finite and positive, signal level changes amplitude without changing field identity, mean luminance remains calibrated, and `uniform-panel-radiance-v1` feeds Light Spread before cover and camera. CPU/Metal coverage includes RGB/BGR, non-origin footprints, placements and character amounts zero, one and four. Diagnostic comparison uses separate uniform 10%, 50% and 80% Device-signal frames plus cumulative PNG16 checkpoints.

Phase-checkpoint coverage proves that requesting an intermediate stops evaluation there even when later physical stages are enabled. Published diagnostics describe that effective stopped execution rather than later authored state: before Photosite/CFA Collection and Noise the sensor is inactive; Collection reports authored collected charge and deferred read noise; Bloom reports coupled charge; Readout reports the final typed RAW; development is active only at Develop and Demosaic. Preview tests forbid diagnostic normalization or visibility compensation: the selected output transform may legitimately clip or conceal correct values without changing the checkpoint. Headless visual comparisons encode the selected display transform as 16-bit PNG so their smooth gradients are not reduced to eight bits; the accepted phase checkpoint remains the typed artifact rather than that presentation file.

Anti-glare microtexture validation requires exact identity at Character zero, finite owner-authored values through Character four, a stable explicit seed, achromatic visibility, positive finite correlation length in physical micrometres and invariance under repeated frames. Its resolved cellular field may modulate only the reflected microfacet population around unit mean; it cannot displace the mean GGX reflection direction, and transmitted panel emission must retain the macroscopic interface and Cover-owned BTDF rather than inherit cellular relief. Moving camera or device must sample the same cover-local realization rather than screen-space noise. Complete and regional evaluation and CPU/Metal output must agree within the optical tolerance. A neutral constant environment must conserve mean reflected energy within that tolerance, and increasing footprint must reduce resolved slope variance without introducing aliasing or disconnected emitters.

The current iPhone-to-ASUS VFX comparison consumes one complete strict JSON fixture under `validation/vfx`. Invocation supplies only that fixture, one explicit external-resource root and a new publication directory; individual physical environment overrides are rejected. Resources are content-verified by SHA-256 before evaluation. Every run publishes a resolved-settings manifest containing the fixture and its hash, resolved resources, Device, pipeline, model contributions, checkpoint, dimensions, timing and output hashes. A candidate parameter change is another complete fixture. Acceptance replaces the current fixture and removes the candidate so the active contract does not retain selectable superseded values. Exposure-to-Sensor area resampling may distribute independent output pixels across workers, but every pixel retains the exact canonical f64 sample traversal and accumulation order; one-worker and multi-worker rasters must therefore be bit-identical.

Moiré-control validation requires Panel-owned structure contrast plus Lens-owned sampled-interference intensity, residual-chroma saturation and optical antialiasing. The accepted Panel Structure checkpoint remains unchanged at every moiré-control value. In the cumulative camera path, intensity zero must select the same projected continuous emitter plus the accepted soft panel glow, removing the periodic carrier before Sensor can sample and re-alias it; intensity one must reconstruct the complete calibrated sampled-panel result exactly. Residual saturation affects only the difference between those two results. Validation requires a phase-by-phase intensity-zero diagnostic through Developed, an ideal full-RGB Sensor control that isolates CFA/RAW sampling, exact filter identity at zero, finite bounds through four, and CPU/Metal agreement for representative combinations.

Device-emission-glow validation requires exact identity at amount zero, finite calibrated and creative values, a bounded panel-white-relative soft threshold, one authored physical radius, no displaced replicas or coherent tap lattice, bloom inside the Device and diffuse additive RGB with zero matte immediately outside its outline. It must consume only accepted Panel Light Spread emission and must not preserve sharp source or subpixel structure in the exterior halo. Environment reflection must be zero outside the antialiased physical cover while transmitted glow remains available there, and reference composition must retain that additive exterior RGB instead of clipping the camera result to the Device polygon. CPU and Metal spatial evaluators must agree within the declared optical tolerance. Every authored analytic environment pattern and its rotation require finite output, exact zero-strength identity and CPU/Metal parity; roughness redistribution uses the owning pattern's approximate spherical mean rather than an unrelated neutral constant. An image-backed source additionally requires exact `environment-image` source/raster exclusivity, a finite exact single-level 2:1 ACEScg raster, explicit radiometric scale, exposure EV and bounded X/Y rotation, and the same cover-interface response as the analytic source. Visible-GGX samples must be finite unit upper-hemisphere normals; zero roughness must select the exact mirror direction. Draft, Medium, High and Native must use respectively 32, 64, 96 and 128 samples of one evaluator. The importance hierarchy must preserve level-zero RGB exactly, conserve every finite non-negative solid-angle-weighted luminance contribution through its one-texel root for both power-of-two and non-power-of-two 2:1 rasters, and never become a radiance lookup. Deterministic output-pixel scrambling must preserve repeated-request hashes while preventing coherent replicated highlights and frame noise. CPU and Metal must agree within the optical tolerance for both lens specializations, neutral incident radiance must remain neutral, and oblique highlight geometry must arise from the view-dependent GGX/Fresnel/Smith integration rather than a circular, elliptical or channel-dependent postblur. Sensor-bloom validation requires exact zero identity, charge conservation except at physical sensor edges, nonrecursive full-well overflow transfer, exact full-frame versus regional equality with the complete two-photosite support halo, bit-exact parallel-gather parity with the serial charge model and invariance under worker-count changes. Reusing dead initial-charge storage for the overflow destination must retain that same bit-exact serial parity, region equality and worker-count invariance. Photosite/CFA Collection and Noise validation covers all four Bayer patterns, nonzero-origin regions, deterministic and stochastic capture, exact selected-row matrix response, canonical raster ordering and bit-identical results across worker counts. Sensor Readout and RAW validation proves that quantization and both clipping masks consume only the coupled-charge checkpoint. The complete-raster identity specialization at the Exposure-to-Sensor boundary must match the general one-to-one area resampler bit for bit, including signed zero, preserve the general fractional-overlap path for unequal rasters and reproduce codes plus both clipping masks for every Bayer pattern.

The exterior-glow gate must use a nonzero authored threshold so CPU and Metal tests detect any regression that attenuates finite-panel support before the bright pass. A neutral balanced-RGB emitter must produce an achromatic halo; the exterior profile must decrease smoothly without repeated plateaus or level-transition bands, retain additive RGB with zero matte, and agree between CPU and Metal at panel-level boundaries and real pixels outside the projected Device silhouette. `Spill exterior` zero must remove external RGB while preserving the covered Device; one is the baseline and four amplifies only external RGB with the same spatial support and CPU/Metal tolerance.

Lens veiling-glare validation requires exact identity at zero, bounded finite authoring, exact
projected-gate clipping, full-frame/region equality and CPU/Metal agreement. Visual matches
against photographs are evaluation evidence rather than numeric golden truth; known camera, lens
and device facts remain separate from assumed exposure, distance and unpublished calibration.

Sensor-boundary coverage supplies one identical shutter-integrated linear checkpoint to the CPU
reference route and the native macOS adapter and requires bit-identical integer RAW codes,
full-well masks and ADC-clipping masks for clean and noisy capture. A separate pre-sensor optical
tolerance may compare independently evaluated CPU and Metal exposure rasters, but it cannot be
converted into a RAW tolerance or used to authorize a second GPU sensor evaluator.
RAW publication coverage requires both clipping masks to be uploaded and published exactly;
Developed ACEScg coverage proves development consumes the same authoritative codes without
requiring an unobservable platform copy of either mask. Complete-route direct texture publication
retains the two ordered reconstruction/development passes, the documented CPU/Metal tolerance and
regional behavior, and must preserve the exact final presentation hash without an intermediate
complete developed buffer or a third publication pass.

The architecture guard also verifies that active documentation does not link into the sealed historical archive and that exact retired paths do not return. It uses Cargo metadata for real package edges rather than accepting source-text conventions.

Contract coverage will reject unknown versions, unknown fields, aliases, missing required values and invalid references. Startup coverage will prove byte-for-byte read-only project opening. Conversion coverage will use disposable copies. CPU/GPU color and numeric implementations will be compared with documented tolerances. Native output publication requires generated OCIO Metal to remain within one 8-bit code value of the pinned CPU oracle and below 0.5% differing channels over the complete adversarial matrix for every current output transform.

Test-authoring cadence crosses Application, the native C ABI and Swift as an exact positive numerator/denominator pair. Coverage includes `24000/1001` and `30000/1001`, which must remain exact through default resolution, editing, recording preparation and persistence instead of being rounded to an integer or transported as floating point.

Scene-resolution conformance constructs one animated scene with independent camera pose, focal/gate/lens and Device tracks and proves that Rust Application materializes one closed `ResolvedSceneFrame` at an exact rational time. Setup, physical Preview, Native, Queue and Fusion must consume that same resolved identity; a compile-time host boundary must make it impossible to pass mutable profiles, tracking assets or a reference raster to a phase evaluator. Full sensor and active window tests include the ARRI 4608×3164 sensor with centered 4608×2592 active crop at global origin `(0,286)`, prove that no pixel scaling occurs and preserve CFA phase for nonzero origins. Full-frame and tiled evaluation must be numerically identical after declared support expansion. Temporal preparation tests cover the global shutter, multiple exact complete-frame sample times and deterministic ordered weights. They also prove linear terminal extrapolation of continuous position, scalar and shortest-arc quaternion curves at both animation boundaries, static one-key/discrete values and boundary-frame retention for sampled media outside its available range. Cache-off, cache-hit, concurrent single-flight, eviction and recomputation must return identical artifacts; no test may accept a nearest-time or reduced-quality result inside the available media range.

Native media decode, timeline authority and every immutable output job share the same positive rational cadence contract. Movie presentation timestamps, audio trim start and audio duration are constructed directly from its numerator and denominator; output publication cannot convert cadence through `Double`, an integer FPS or an arbitrary timescale.

Native VFX-output coverage requires both ProRes 4444 and ProRes 4444 XQ presets, one explicit stable Log/Gamut id and exact resolution of every catalog entry through the pinned OCIO configuration. Camera recommendations are complete optional ids but never availability filters. The immutable job round trip preserves the authored codec and Log/Gamut after the global preset changes. The writer accepts VFX Log/Gamut only for those two ProRes codecs, rejects missing or unknown ids and does not publish a Rec.709 proposal for the encoded movie.

Recording-output color coverage requires the stable iPhone HEIC transform id to resolve exactly and rejects labels, filenames and extensions as aliases. Its `recording-output-signal-v2` artifact must carry the exact complete P3-D65/sRGB/RGB/full-range declaration, opaque alpha and one finite nonlinear RGBA float per raster pixel before quantization or chroma subsampling. A contradictory BT.709 RGB transfer declaration is rejected. The adapter must pass those codes to a Display P3 RGB color space; codec-owned YCbCr conversion and signaling cannot redefine the upstream RGB artifact. The artifact rejects a wrong id, incomplete or contradictory color declaration, non-opaque alpha, non-finite samples and raster-length mismatch. Preview ODT selection cannot change this boundary.

Recording-codec coverage requires exact profile identity, an explicit output transform for every profile, complete capture-preset recommendations and availability of every bundled profile for every camera. Changing camera resets to its exact default profile without hiding alternatives. Character zero is codec bypass, one is calibrated and values above one increase codec character without mutating Recording Output. Every requested frame is one real independent encode/decode operation: no profile or execution contract contains GOP, B-frame, lookahead or whole-clip state. Tests execute H.264 High 8-bit, HEVC Main10, ProRes 422 HQ and ProRes 4444 through AVFoundation, prove nonempty payloads and decoded finite rasters, and require Rec.2100 PQ for the three HDR-capable video profiles while H.264 uses Rec.709. Still HEIC/JPEG use ImageIO. The macOS ImageIO and AVFoundation adapters publish a distinct decoded raster, encoded byte count and SHA-256 payload identity. Their decoded signal returns through the exact inverse recording transform before the ordinary monitor Preview transform. An opaque ProRes 422 round trip is tested with physical matte values zero, one half and one: the matte bypasses the codec exactly, decoded additive RGB remains nonzero at matte zero and the final reference equation uses the unchanged matte without encoding the plate. Codec-only changes reuse the camera checkpoint, Output changes invalidate both downstream artifacts and upstream changes require a new physical checkpoint.

The canonical iPhone HEIC profile uses constant quality `0.82`. The accepted codec diagnostic fixes that value together with the recording-output transform, decoded raster, encoded byte count and payload hash; a later change requires a new explicit comparison and acceptance.

Source-to-feeder coverage requires every authored Input Transform to declare its reference domain, every Output Signal to resolve both its scene-rendering and display-colorimetric OCIO processors, and equal sRGB input/output to traverse ACEScg without an identity route. Initial-import tests cover Source and Reference, movie and still, metadata with no missing fields, partially labelled media and wholly unlabelled alpha media. They prove detected fields remain unchanged; only absent fields receive the disclosed sRGB/Y′CbCr-BT.709-video or sRGB/RGB-full defaults; present alpha defaults to Premultiplied and absent alpha to Ignore/Opaque; every such field remains marked proposed and editable. Reopening persisted authoring must not reapply those defaults. The strict feeder color checkpoint is RGBA16F with exact Input Transform, Output Signal, resolved processor and alpha identities. Swift and Rust readers reject unknown fields, unknown files, contradictory transform ids and incorrect raster length. Changing Device, Color Mode, White Luminance, placement or Preview quality must leave that color checkpoint byte-identical. Separate feeder-composition tests prove Fit and Fill/Crop preserve aspect, Stretch alone may deform, One-to-One maps one source pixel to one target pixel, and target Device dimensions or placement change `placed-feeder-signal-v1` without recoding the color checkpoint. Device-interpretation coverage proves matching signal/mode pairs preserve intended response and intentional mismatches change panel emission. Panel-structure coverage proves the cumulative checkpoint stops at subpixel radiance before spread, cover and camera. Diagnostic PNGs may differ by at most one 8-bit code value per channel under the existing OCIO CPU/Metal tolerance.

Desktop export coverage decodes an in-memory PNG and requires its raster and RGBA bytes to equal the immutable Native level-zero publication buffer exactly. Display-pyramid tests separately require exact 2× area reduction, including odd raster edges, so export cannot silently consume a presentation level.

Saved-frame settings coverage requires one strict current snapshot containing Device, pipeline, model contributions, source interpretation/placement, the complete resolved Test selection, Preview transform/phase, explicit Environment resource identity and explicit reference resource identity plus its ordered match pixels. Its encode/decode round trip is exact and retired schemas, missing fields and malformed resource identities fail. Image Environment and reference import require the recorded SHA-256; same-name substitution and inferred transforms are forbidden. Viewer navigation, transport, source pixels and observed monitor profile remain outside the imported render state.

Saved-scene coverage requires the single strict v21 scene-library schema, unique scene ids, nonempty editable names, exactly one stable selected id per profile family, unique typed fixed overrides, sparse physical-model overrides, current frame and finite Viewer fit/zoom/pan. It rejects a dense resolved Test selection, a duplicated profile identity, a full profile snapshot, unknown nested fields and incompatible override values. Global Library contract tests cover Device, Cover Glass, Camera/Sensor, Lens and procedural Environment together: each locked bundled seed can be duplicated into an ordinary editable record; each user-created identity resolves through the same typed Application context; duplicate ids and dangling cross-family associations fail. A Saved Scene integration test changes a current default independently in every profile family after Save and proves each value without override follows it; stored overrides remain authoritative and Reset removes them. A live-library integration test commits simultaneous changes in more than one profile family and proves that the active scene rematerializes every selected identity from one complete new library snapshot, retains explicit overrides and publishes one rebuild rather than a field patch; a missing selected identity leaves the active scene unchanged and reports the failure. Authoring-interaction coverage sends equivalent stable parameter edits through a descriptor card, Device placement, camera navigation, Focus target, finite-Environment gesture and Reference Match, then proves each route produces the same explicit override semantics without recording the editor identity. A value explicitly edited to its current default remains stored, Reset alone removes it, multi-parameter gestures commit one atomic override set and Undo/Redo restore both values and override presence. Source inspection rejects inference of overrides from resolved-value differences and direct gizmo mutation of persisted override storage. Source inspection additionally rejects any host-side runtime lookup into bundled Device, Cover, Camera, Lens, Environment or output-preset seed catalogs after the Global Library has been loaded. Imported 3D authoring is scene-owned: the complete typed camera curves, focal/gate/plate/distortion contract, point groups, polygon geometry and face cardinality are embedded together with explicit camera and point-group ids, independent point-cloud and geometry visibility, unique visible-mesh ids and positive metric calibration. Moving or deleting the temporary importer file after Save cannot change or invalidate the scene. A source is exclusively either one valid synthetic-pattern id or one or more absolute external media paths accompanied by required persisted raster and timing facts. The only permitted external scene dependencies are source media, reference media and an authored external HDRI; each preserves its exact path and an unavailable required path rejects Open. Generated reflection HDRIs are scene-owned media assets. Missing Environment or reference paths, absent thumbnails, unknown fields and old or future versions fail without modifying the library or active scene. Save and Update write the thumbnail and complete authoring before publishing the index. Open validates and finishes loading exact external HDRI and reference paths before publishing scene-open success; a pending asynchronous resource load is not a materialized scene. Normal library loading performs no migration. A schema advance is covered by a separate selected one-way maintenance conversion over a disposable copy and the resulting current library must pass the same strict reader.

Saved-scene render-queue coverage requires enqueueing only a complete saved scene. An active scene whose current authoring differs from the stored scene must require explicit Update before enqueue; an unsaved working state is rejected. A required options sheet precedes destination selection and resolves composition, exact frame range, compatible output target/format and motion-blur enable/sample count. The queued job owns a value copy of the scene ids, overrides, tracks and resources, any scene-owned generated Environment bytes and the frozen output configuration; a later rename, Update, Open, Delete, current UI profile selection or new scene override cannot change that copy. Each attempt resolves current profile definitions for the job's stored ids once in its isolated workspace and freezes them for the attempt. A later retry may see changed defaults for those same ids, while an absent id fails rather than substituting the current UI profile. Evaluation requests the camera-rendered checkpoint for each exact frame, applies the saved Delivery Raster once and never samples the visible workspace's mutable source or physical state. Enabled motion blur uses the saved physical shutter interval with 2–64 temporal samples; disabled motion blur resolves one sample and no post-process blur. `Solo Device` publishes that Delivery Raster directly. `Device + referencia` requires the saved reference, samples it at the same rational frame time and composites it after Delivery Raster; it must leave the physical request and camera-rendered checkpoint identical to `Solo Device`.

Render Queue workstation-state coverage requires atomic round-trip persistence of job UUID/order, frozen scene, generated Environment bytes, output plan/manifest, resolved configuration including overwrite policy, lifecycle detail/progress and paused state. Invalid or unknown queue-document fields fail. A job persisted as rendering restores as pending with an explicit interruption detail and no active task; it cannot begin until the user explicitly runs the queue. Bulk cleanup removes completed and failed records together while retaining pending, rendering and cancelled records. Context removal accepts pending, failed and completed records only. Both operations leave generated output files untouched.

Photometric coverage owns one explicit device-code scale and verifies every patch is exactly achromatic. A uniform half-code panel with zero authored black level must retain the exact authored EOTF ratio relative to white after physical optical evaluation. Separate owning-domain tests verify analytic shutter/ND scaling, photosite quantization and clipping identity, explicit middle-gray development placement, and parity between the ideal camera preview and the complete Native RAW/develop route for the same calibrated exposure. The pinned ACES 2.0 sRGB SDR output also owns fixed middle-gray and diffuse-white code anchors. No photographic file is a golden value for these tests.

Capture-preset coverage requires one explicit default Lens evaluation model alongside the compatible lens association. Changing capture preset proves that iPhone restores VFX 2D while ARRI restores direct Thin Lens, and the Lens phase publishes that resolved choice; the host cannot infer it from identity, quality or raster size.

Lens coverage requires unique stable template ids, nominal focal length, complete finite coefficients including physical center/edge softness, successful full-gate inversion certification for every bundled model and valid midpoint interpolation across the current catalog. Every capture template must declare a nonempty compatible-lens set containing its default. Every compatible id must resolve in the single lens catalog; a fixed association must contain only its default. Capture templates contain no duplicate focal length or aberration authority. Each distinct shifted and Y-flipped observed coordinate is inverted once; the resulting valid unscaled ideal coordinate is reused across channels, pupil samples and irradiance, while every VFX ±X/±Y coordinate retains its own validity and channel scaling acts only on a copy. A pupil sample prepares one world and screen-local lens origin for all of its channel and footprint rays; the inverse screen quaternion and VFX rim origins are shared only where their inputs are exactly identical. The ideal-point natural-vignetting and pupil-throughput weight is invariant across direct pupil samples and must be reused without changing its channel-specific value. PSF coverage requires physical softness to grow correctly with sensor sampling density, authored image height and f-number, and requires its circular quadrature to remain centered, bounded and unit-normalized. Direct-pupil coverage requires exactly 32 equal-weight aperture rays independent from every sensor-footprint and PSF sample, one stable global pupil orientation with identical CPU/Metal ray order, per-channel chromatic geometry before photosite/CFA collection and CPU/Metal optical parity. The Metal product backend specializes the same owned kernel into the Cartesian product of Thin Lens and VFX 2D with procedural and equirectangular environment sources. Tests require the immutable typed plan to select the matching one of four pipelines, forbid evaluator and environment-source selectors in the parameter buffer, retain CPU/Metal parity for zero, calibrated and exaggerated lens amounts in both evaluators, and exercise both source kinds through each lens specialization. A focused chromatic edge must remain spatially coherent instead of acquiring pixel-dependent color displacement from finite pupil quadrature. The frozen cross-quality matrix requires High and Native to remain within `3e-5` maximum absolute channel difference; this convergence bound does not relax the separately declared CPU/Metal optical tolerance. Complete-frame row-prefix coverage requires fractional source-edge weights, placement clipping, negative and above-one values and wide optical footprints to remain within the existing CPU/Metal optical tolerance; a prefix representation cannot relax the tolerance or select another result. A full Lens-to-Sensor exposure invariant proves the inverse-square f-number term is applied exactly once: two stops down yields one quarter of the RAW exposure at fixed shutter and matches after four-times shutter compensation before clipping.

The VFX 2D depth-of-field approximation uses the same coherent chief-ray chromatic projection and replaces only the discrete pupil axis with one deterministic centered continuous footprint. Its VFX-only PSF combines authored softness and Airy radius by root-sum-square variance matching without changing the canonical direct-pupil thin-lens PSF. Its footprint must be exactly repeatable, collapse for a channel where that channel's authored focus plane meets the chief ray and grow continuously away from that condition without inventing a post-sensor blur or another CFA route. The sensor-pixel footprint is projected into panel UV per channel by chief rays at both sides of each sensor axis before it can combine with the pupil footprint; changing distance, angle or gate coverage must therefore change panel support through geometry rather than through a presentation-space constant. The projected sensor-pixel and pupil half-extents add independently per axis as convolution support: zero pupil extent preserves the exact projected sensor footprint and a nonzero pupil grows continuously and monotonically. For the owned vertical RGB-stripe topology, the VFX-only carrier residual samples the canonical device signal through exactly one quarter of the projected horizontal sensor-pixel half-extent and the complete projected vertical sensor-pixel half-extent at the coherent chief ray. The pupil footprint belongs exclusively to the continuous content envelope and cannot broaden or replicate this carrier residual. The carrier is multiplied only by the authored Panel Structure amount and must preserve analytic black-matrix coverage rather than select a narrow phase that can miss the row margin. It cannot reuse the complete defocus-footprint averaged device code, alter the content envelope, change the canonical direct-pupil evaluator or move into a post-sensor stage. Photosite/CFA Collection and Noise remains the sole owner of camera sampling and aliasing; demosaic remains in Develop after RAW readout.

Device-emission-glow coverage requires exact identity at zero, finite calibrated and creative
strength, a bounded panel-white-relative threshold, bloom inside the Device, continued diffuse RGB
with zero matte just outside the projected active outline and CPU/Metal parity for the centered
one keyed exponential-radius support over accepted Panel Light Spread emission. The halo cannot read original source,
reference or Environment pixels, preserve sharp subpixel structure, crop at the active outline or
move after photosite/CFA collection.

Test focus coverage requires Autofocus to store a finite normalized Device-local target, transform it through the current rigid Device pose and resolve its positive optical-axis depth from the current camera pose at every evaluated frame. Rearward and out-of-domain results fail explicitly. Manual focus preserves its independent authored value. The Lens phase publishes Autofocus, both target coordinates and f-number with camera-preset reset values, hides target and manual-distance controls according to the active policy, and never infers a missing target. The Exposure phase publishes real exposure time and changing f-number or exposure time must alter the same immutable physical request consumed by interactive and reference evaluation. Exported physical settings record the focus policy, target and resolved distance. Every phase publishes a nonempty owner-authored perceptual summary; character header ownership is explicit and cannot be reconstructed from control ids by Swift.

Spatial Metal coverage includes a full-resolution coordinate-phase case rather than testing only
small origin-adjacent rasters. The current 8064×6048 iPhone sensor, integrated lens and 3840×2160
ASUS panel must remain within the CPU optical oracle tolerance at a non-origin resolved-subpixel
region. Spatial shader compilation must use safe math with floating-point contraction disabled in
both Cargo and packaged shader builds; phase-unstable fast math is not a supported quality level.

Optical-cover coverage requires unique stable current cover and analytic-HDR environment ids, valid complete material profiles and successful resolution of every device template's default cover id. The display-cover family must order glossy strong AR, glossy standard AR, semi-gloss, calibrated matte AR and heavy matte monotonically by roughness, haze and microtexture RMS slope; the matte category remains the fixed calibration anchor, while thick glossy glass is excluded from that finish ordering. Zero cover character must be bit-exact identity, zero environment character must contribute no reflection, IOR 1 and perfect AR must produce exact zero interface reflection even at grazing incidence, and the calibration grid must lose contrast as roughness redistributes its energy. Every analytic distribution and a nonzero rotation require CPU/Metal spatial parity under the current optical tolerance. Cover changes must affect Composite without changing the pre-cover Emitted Radiance diagnostic. Draft and Native consume the same authored cover/environment request and evaluate it over the same aperture-ray set.

Reflection-environment authoring coverage requires unique typed circular-practical, convex-window and sun emitters, finite normalized directions, bounded non-negative radiance, explicit temperature/tint, and a bounded exact 2:1 output raster. A practical is a spherical disk defined by one center direction and one angular diameter; its explicit distance derives one plausible physical diameter. A window uses four ordered directions and explicit distance. Neither claims that the generated HDRI retains parallax; a sun has angular diameter and no distance. Compiling the same rig is deterministic, keeps the background and every output channel finite and non-negative, preserves one connected spherical source across the equirectangular seam, and writes only an explicit linear-ACEScg Environment asset. An unsaved working state overwrites one working asset. A saved scene overwrites exactly one map owned by that scene; duplication must copy its bytes to a distinct scene-owned identity and deletion must remove only the deleted scene's map. Tests verify stable paths, changed hashes after overwrite, independent duplicate identities and strict missing/hash rejection. The workstation must select the resulting file through `environment-image`; the tool cannot publish a physical checkpoint, paint a post-process reflection or bypass the existing Cover/Environment evaluator.

Planar HDRI framing additionally requires finite pan, positive zoom and finite roll, an exact 2:1 scene-linear source, and an explicit current camera/Device pose. The planar preview and generated environment must agree at the Device centre and corners for an ideal mirror within the declared sampling tolerance. Repeated input is deterministic; source rotation is baked exactly once, placement resets to zero rotation and distant projection, and pixels outside the Device-mapped solid angle retain the rotated original environment. Moving camera or Device after generation must change the reflection normally. The tool may only publish a managed OpenEXR selected through `environment-image`; no framing state or pose lock may enter Cover/Environment.

The default cumulative Preview contract selects the final published phase. Native-render freshness coverage distinguishes an invalidated physical revision, the current revision rendering, a cancellation requested but not yet confirmed by the Metal worker, and a completed current physical revision. Replacing the source pattern or media frame and selecting a different cumulative physical phase must invalidate a completed Native result exactly as a physical-parameter edit does. Swift presentation must not construct a parallel source/profile identity or reject a result already validated by the physical request revision and hash. Active-worker progress is capped below 100%; terminal coverage requires the result texture to be published before the observable task lifecycle ends and the control becomes green. Cancellation coverage proves that no replacement Native job can be submitted during that confirmation interval. Viewer coverage requires a fixed one-point frame outline around the transformed raster independently of Fit scale, zoom and pan. Setup and Reference-composite coverage requires fractional edge pixels along oblique and lens-distorted Device silhouettes while full interior and exterior samples remain unchanged; the presentation-only operation cannot alter a physical checkpoint, monitor output or saved frame.

Camera-navigation coverage proves Pan world displacement scales with optical depth while normalized projected displacement remains constant, Dolly changes depth proportionally and cannot cross the Device plane/near bound, and horizontal/vertical Orbit locks exactly one Device-local axis while preserving camera-to-center distance. A transformed Device supplies the world center, axes and four rigid corners but no navigation gesture may mutate its pose. Navigation accepts a programmatically supplied external camera pose, writes the existing Free Geometry controls and commits one complete gesture as one Undo operation. Interactive input coalesces to the latest pending pose, flushes only the final pose at gesture end and rejects wheel momentum so no queued motion can continue after release. Viewer presentation pan/zoom remains independent from physical camera navigation.

Reference-match coverage proves every supported Delivery Raster mapping is exactly invertible between Delivery-Raster targets and selected-camera gates, including different aspect ratios. Reference input coverage proves Input Transform, alpha interpretation, signal model, YUV matrix, signal range and `Fit`/`FillCrop`/`Stretch`/`OneToOne` placement are explicit and independently authored from the Device source. Initial yellow targets remain inside the Delivery Raster independently of reference letterboxing and an offscreen Device projection. Brown-Conrady targets convert to equivalent pinhole coordinates before the calibrated planar solver; the resulting rigid camera pose is then checked through the authored distortion and lens shift in Delivery-Raster pixels. The four yellow targets and their quadrilateral remain invariant when the red projected corners change, moving one target cannot mutate the other three and no camera pose is committed before four correspondences exist. Every target accepts a direct drag without a modifier and resolves the rigid camera only when that drag ends. Opening the Match panel exposes every target, line and projected cross; closing or hiding it hides all those overlays while preserving target coordinates for reopening. A four-point solve updates only camera pose, reports maximum reprojection error and commits one Undo operation; Device corners remain byte-for-byte unchanged and degenerate, behind-camera or non-invertible configurations fail explicitly. Match input coverage disables physical Pan/Orbit/Dolly while retaining presentation Pan and wheel/pinch/Shift-MMB Zoom. A still reference leaves source timing authoritative; a movie reference supplies exact finite timeline frame rate and duration while loaded, drives time-indexed reference decode and restores source timing only when removed. The reference background remains published after Match is disabled and across Setup and evaluated Viewer results; its presentation-only composite cannot enter monitor output or a rendered artifact. The native bridge never exposes or persists a deforming corner-pin matrix.

Focus-Setup coverage requires black outside the Device, white at zero circle of confusion, monotonic darkening as defocus grows, and sensitivity to the authored focal length, f-number and resolved focus distance. An animated imported camera must produce a newly resolved autofocus distance at each exact frame; the authoring layer preserves the independent manual distance rather than baking the tracked-camera result into it. Its red chart cannot escape the Device and both chart and sampled one-pixel boundary must follow the same radial/tangential distortion coefficients while camera navigation leaves the Device transform invariant. Projecting and inverse-picking the green autofocus target must round-trip its Device UV through nontrivial gate placement, lens shift and Brown-Conrady distortion; one complete drag creates one undo entry, cannot move the camera or Device, and an imported animated camera must re-resolve the same local target at the current exact frame.

Broad tests are never a fallback for an unclassified change. The validation-owner manifest must first assign the changed path to its semantic owner and exact checks.

Tracking-overlay conformance projects point-cloud vertices, polygon edges and mesh centres through the same `ResolvedSceneFrame`, active sensor window and Delivery Raster placement used by the underlying image. Its output identity must equal the published image revision, frame index and exact rational time before the host may draw it. Scaling the imported world scales camera translation, geometry and the anchored Device position by the same factor while retaining the Device's physical dimensions; the overlay anchor and rendered Device centre therefore remain coincident across frames and scales. UI and host targets cannot call a second camera projector.

Undo conformance performs at least two consecutive authored edits, undoes them independently to both prior values and redoes them independently to both later values. It proves each logical action closes its own synchronous group and that inverse registration occurs while `UndoManager` is actively undoing or redoing; yielding to an asynchronous task for inverse registration is forbidden.

Device-outline coverage proves zero-radius rectangular identity and rounded-corner rejection at the physical corner in both the CPU oracle and Metal product evaluator. Setup uses the same radius for coverage and picking, while Fusion receives the physically masked RGB/matte raster on a transparent rectangular carrier; no consumer may recreate another outline.
